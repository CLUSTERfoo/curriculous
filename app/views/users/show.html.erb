<% content_for :page_title, @user.username %>

<h3 class="subheader"><%= @user.username.capitalize %></h3>
<hr>

<% if @user.memos.any? %>
    <% @memos.each do |memo| %>
        <%= render partial: "shared/memo", locals: { memo: memo } %>
    <% end %>
    <%= will_paginate @memos %>
<% end %>
