<h3 class="subheader">Latest Memos:</h3>
<hr>

<% if @memos.any? %>
    <% @memos.each do |memo| %>
        <%= render partial: "shared/memo_min", locals: { memo: memo } %>
    <% end %>
    <%= will_paginate @memos %>
<% end %>

